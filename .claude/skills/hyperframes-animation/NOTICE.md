# Provenance

Vendored from [`heygen-com/hyperframes`](https://github.com/heygen-com/hyperframes)
(`skills/hyperframes-animation/`), commit `dd88b5f5b78f25a82ad0677c00bbbfa85428454f`.

Installed because the full `/brag` skill (`.claude/skills/brag/SKILL.md`,
step 3: "Hand off to Hyperframes") names this as one of the Hyperframes
domain skills to read before composing the render. HyperFrames is the
engine `/brag` hands its composition brief to.

To update: `npx hyperframes skills update`, or re-copy `skills/hyperframes-animation/`
from the upstream repo. License: Apache-2.0 (see `LICENSE` in this
directory).
